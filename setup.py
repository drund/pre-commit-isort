from setuptools import setup

setup(
    name="isort",
    version='0.3.0',
    description="isort hook.",
    py_modules=["isort"],
    python_requires=">=3.7",
    install_requires=[
        "isort",
    ],
)
